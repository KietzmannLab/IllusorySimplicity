import setuptools

setuptools.setup(
    name="macaquethings",
    version="1.0",
    description="A library of useful functions for TVSD and companion datasets",
    packages=setuptools.find_packages(),
    install_requires=[],
)
